//////////////////////////////////////////////////////////////////////
//====================================================================
//
//	Framework of LunAtic Notation
//	(c)2006 danmaq
//
//		※各上位ライブラリから間接的にインクルードされます。
//			直接インクルードすることはありません。
//
//====================================================================
//////////////////////////////////////////////////////////////////////


//////////////////////////////////////////////////////////////////////
//	定数(書換不可)
//////////////////////////////////////////////////////////////////////


//////////////////////////////////////////////////////////////////////
//	定数(書換可能)
//////////////////////////////////////////////////////////////////////

/**	初期ボム数を設定します。 */
let FLAN_PLAYER_BOMB					= 3;
/**	喰らいボム初期時間を設定します。 */
let FLAN_PLAYER_BORDERLIFE_DEFAULT		= 15;
/**	喰らいボム減少量を設定します。
*	負数を設定すると可変量減少となります。
*	(減少量算出式: (被弾～喰らいボム発動に費やした時間)*(-FLAN_PLAYER_BORDERLIFE_REDUCE) )
*/
let FLAN_PLAYER_BORDERLIFE_REDUCE		= -1;
/**	一度ショットを撃ち始めたら最低撃ち続ける時間を設定します。 */
let FLAN_PLAYER_SHOT_FRAME				= 30;
/**	ショットのフレーム間隔を設定します。 */
let FLAN_PLAYER_SHOT_INTERVAL			= 5;
/**	自機高速移動速度を設定します。(dot/frame) */
let FLAN_PLAYER_SPEED_FAST				= 4;
/**	自機低速移動速度を設定します。(dot/frame) */
let FLAN_PLAYER_SPEED_SLOW				= 1.6;
/**	ショットのフレーム間隔を設定します。 */
let FLAN_PLAYER_COLLISION_HIT			= 0.5;
/**	自機カスり判定の半径を設定します。 */
let FLAN_PLAYER_COLLISION_GRAZE			= 40;
/**	アイテム自動蒐集ラインを設定します。 */
let FLAN_PLAYER_ITEM_AUTOCOLLECT_LINE	= 128;
/**	ボム時のアイテム全蒐集を有効にするかどうか設定します。 */
let FLAN_PLAYER_ITEM_AUTOCOLLECT_BOMB	= true;
/**	被弾時に被弾した弾を即座に削除します。 */
let FLAN_PLAYER_DELETE_KILLED_SHOT		= true;


//////////////////////////////////////////////////////////////////////
//	メンバ変数(書換不可)
//////////////////////////////////////////////////////////////////////

/**	現在のフレームでショットが発射可能かどうかが格納されます。 */
let m_bFlanPlayerShotEnable = false;
/**	現在ショット中かどうかが格納されます。 */
let m_bFranPlayerShot = false;
/**	喰らいボム時間使用量が格納されます。 */
let m_nFlanBorderLifeUsedCount = 0;
/**	ミス時のカウンタが格納されます。 */
let m_nFlanPlayerMissedCount = 0;


//////////////////////////////////////////////////////////////////////
//--------------------------------------------------------------------
//	共通処理
//--------------------------------------------------------------------
//////////////////////////////////////////////////////////////////////

/**
*	該当スクリプトのメインタスク呼び出し処理です。
*	注意:自動的に内部から呼ばれますので直接呼び出すことは通常ありません。
*/
task __MainTask(){
	yield;
	MainTask();
}
